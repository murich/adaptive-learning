export enum KnowledgeType {
  LETTER = 'letter',
  SYLLABLE = 'syllable',
  WORD = 'word',
  PHRASE = 'phrase',
  CUSTOM = 'custom',
}

export enum DependencyType {
  PREREQUISITE = 'prerequisite',
  SUPPORTS = 'supports',
  RELATED = 'related',
}
